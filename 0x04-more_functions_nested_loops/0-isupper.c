#include "main.h"
/**
 * _isupper - check the code.
 * @c: variable to be tested
 * Return: Always 0.
 */

int _isupper(int c)
{
	if (c == 65)
		return (1);
	else if (c == 97)
		return (0);
	return (0);
}
